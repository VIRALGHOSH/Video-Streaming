@model Video_Streaming.Models.Model_feedback

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_UserMaster.cshtml";
}

<div class="row">
	<div class="col-xs-12">
    @using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
		<div id="form" class="form-horizontal">
			<section class="panel">
				<header class="panel-heading">
					<div class="panel-actions">
						<a href="forms-validation-state.html#" class="panel-action panel-action-toggle" data-panel-toggle></a>
						<a href="forms-validation-state.html#" class="panel-action panel-action-dismiss" data-panel-dismiss></a>
					</div>
                    <h2 class="panel-title">Feedback</h2>
					<p class="panel-subtitle">
						Basic validation will display a label with the error after the form control.
					</p>
				</header>
				<div class="panel-body">
			
					<div class="form-group">
						<label class="col-sm-3 control-label">Login id<span class="required">*</span></label>
						<div class="col-sm-9">
							@Html.TextBoxFor(model => model.login_id, new { @class = "form-control" })
                            @Html.ValidationMessageFor(model => model.login_id)
						</div>
					</div>
                
                    <div class="form-group">
                        <label class="col-sm-3 control-label">Feedback<span class="required">*</span></label>
						    <div class="col-sm-9">
							@Html.TextAreaFor(model => model.f_des, new { @class="form-control", @rows="4", @title="Please enter a Feedback ." ,@placeholder="Your Feedback description" })
                            @Html.ValidationMessageFor(model => model.f_des)
						    </div>
					    </div>`
				</div>
				<footer class="panel-footer">
					<div class="row">
						<div class="col-sm-9 col-sm-offset-3">
							<button type="submit" class="btn btn-primary">Submit</button>
							<button type="reset" class="btn btn-default">Reset</button>
						</div>
					</div>
				</footer>
			</section>
		</div>
}
</div>
</div>

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
